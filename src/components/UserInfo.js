import { List } from 'semantic-ui-react'

export default function UserInfo({ user }){

    // Render item user property of type string as using the following item 
    // <List.Item>
    //     <List.Header>{_}</List.Header>{user[_]}
    // </List.Item>

    return(
        <List>
        </List>
    )

}